# Eightfold Candidate Profile Transformer Configuration
# Copy to .env and adjust variables as needed

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Default phone region for parsing numbers without country code
DEFAULT_PHONE_REGION=US
